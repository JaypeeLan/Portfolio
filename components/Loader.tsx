import React from "react";

const Loader = () => {
  return (
    <div id="loader">
      <div id="logo">
        <img src="/logo.png" height={300} width={300} alt="logo" />
      </div>
    </div>
  );
};

export default Loader;
